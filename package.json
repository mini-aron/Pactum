{
  "name": "pactum",
  "private": true,
  "version": "0.0.0",
  "description": "Pactum electronic contract library monorepo",
  "license": "MIT",
  "keywords": [
    "contracts",
    "pdf",
    "canvas",
    "react",
    "typescript",
    "e-signature"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  }
}
